<% @quest.qs.each do |q| %>

<a href = <%= '/quests/'+@quest.id.to_s()+'/qs/'+q.id.to_s()%>> 
<div class ="post">
  
   <%= q.title %>
  <div class="time"> <%= q.created_at.strftime("%b,%d")%>
  </div>
 </div></a>
<hr class = "line">
</hr>
 <% end %>
