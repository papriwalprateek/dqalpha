<div id = "rightcontent">
<%if current_user%>
<%if controller.controller_name =="quests"%>

<%if params[:id]%>
<%quest=Quest.find(params[:id])%>
<%if quest%>
Quesers in this Quest:
</br>
<table>
<% quest.users.each do |user| %>
<tr><%= link_to h(image_tag(Gravatar.new(user.email).image_url, :size => "50x40",:align=>"left")), user %></tr>

<%end%>
<%end%>
<tr>
<button class="button1" id="Add" >  

+</button>

</tr>
</table>
<%end%>
<div id="dialog-form" >
<%= form_tag(:controller=> "quests",:action => "show") do %>
  <%= label_tag(:add_user) %>
  <%= text_field_tag :add_user %>
  <%= submit_tag("Add") %>
<% end %>
</div>
<hr>
<%end%>

<%end%>
<table>
<tr><%= image_tag("wiki.png", :alt => "Wikipedia", :height => "40", :width => "50") %></tr>
<tr><%= image_tag("gist.png", :alt => "Gist", :height => "40", :width => "50") %></tr>
<tr><%= image_tag("so.png", :alt => "Stack_Of", :height => "40", :width => "50") %></tr>
</table>


<%= form_tag({:controller=> "pages",:action => "wik"},:remote =>true, :method => "get") do %>
  <%= label_tag(:ad, "Search")%>
  <%= text_field_tag :ad %>
  <%= submit_tag("Wiki") %>
<% end %>

</br>


<div id ="wik">

<%= render 'layouts/wiki' %> 
</div>

</div>
	<script>
	
	
		
	$(function() {

		//autocomplete for user names
		var availableTags = [<% for user in User.all -%>
		"<%= user.name %>"
		<%if user==User.last%>
		<%else%>
		,<%end%>
		<% end -%>];
		$( "#add_user" ).autocomplete({
			source: availableTags
			});
		
		
		//dialog box for adding user
		$( "#dialog:ui-dialog" ).dialog( "destroy" );
		
		var name = $( "#name" );

		$( "#dialog-form" ).dialog({
			dialogClass: 'ui-dialog-shadow',
			autoOpen: false,
			height: 100,
			width: 200,
	
			modal: true,
			buttons: {
				
			},
			close: function() {
				
			}
		});

		$( "#Add" )
			.button()
			.click(function() {
				$( "#dialog-form" ).dialog( "open");
			});
	// hide it first
	
	});
	</script>


