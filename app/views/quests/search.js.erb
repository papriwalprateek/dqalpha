$(".quest_search_page").html("<%= escape_javascript(render("search_page")) %>");
//$("span.vm_icon1").hide();
var js_array = [<%= raw @arr.to_json %>];
for (var i=0;i<js_array.length;i++)
{ $("span#"+js_array[i]).show();
}
$("span#"+js_array[0]).click();
<%if @query%>
  window.history.pushState(null, document.title, "<%="/quests/"+@quest.id.to_s()+"/search?search="+@query.to_s()%>");   
<%end%>
