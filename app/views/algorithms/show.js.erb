$(".quest_search_page").html("<%= escape_javascript(render("result")) %>");
<%if @query%>
  window.history.pushState(null, document.title, "<%="/search?search="+@query.gsub('+',"%2b")%>");   
  $('p.searching_text').hide();
ga('send', 'pageview', {'page': "<%="/search?search="+@query.to_s()%>",'title': "<%=@query%>"});
<%end%>
